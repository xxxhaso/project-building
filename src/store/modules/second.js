const state = {
  // 初始化数据
  secondNum: 300
}
const getters = {
// 类似于computed属性
// get 获取状态
}
const mutations = {
  // 同步方法
}
const actions = {
  // 异步方法
}
export default {
  namespaced: true, // 进行多模块处理
  state,
  getters,
  mutations,
  actions
}
